import { Box, styled } from '@mui/material';

const Root = styled(Box)`
  height: 100vh;
  width: 100vw;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4rem;

  padding: 3rem;
`;

export default Root;
